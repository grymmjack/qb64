'$INCLUDE:'../../../QB64_GJ_LIB/_GJ_LIB.BI'

SCREEN 0'_NEWIMAGE(1024, 768, 256)
console_setup

GJ_LIB_ANSI_EMU    = FALSE
GJ_LIB_ANSI_OUTPUT = TRUE

PRINT PIPEPRINT( _
    "|15|25|@D00 |CR|@D00 |CR|@D00 |[Y02|@C{LIGHTBAR MENU}|16" _
)

BOX_EDGE$ = "|@C{|27 " + STRING$(80, " ") + " |16}"
BOX_SIDE$ = "|@C{|27 |16" + STRING$(80, " ") + "|27 |16}"

PRINT:PRINT
PRINT PIPEPRINT(BOX_EDGE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_SIDE$)
PRINT PIPEPRINT(BOX_EDGE$)

PRINT PIPEPRINT("|CR|@C{|[Y22|[X01|11You |07can |09do |14a lot|12!}")
PRINT PIPEPRINT("|[Y23|[X01|[1|16")

'$INCLUDE:'../../../QB64_GJ_LIB/_GJ_LIB.BM'